This folder holds simple SQL migration files for reference.

For a full Alembic setup, run:

    alembic init alembic

Then copy `schemas.sql` contents into your migration or use `alembic revision --autogenerate`.
